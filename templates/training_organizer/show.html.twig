{% extends 'base.html.twig' %}

{% block title %}TrainingOrganizer{% endblock %}

{% block body %}
    <h1>TrainingOrganizer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_organizer.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ training_organizer.createdAt ? training_organizer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_organizer_index') }}">back to list</a>

    <a href="{{ path('app_training_organizer_edit', {'id': training_organizer.id}) }}">edit</a>

    {{ include('training_organizer/_delete_form.html.twig') }}
{% endblock %}

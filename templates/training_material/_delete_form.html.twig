<form method="post" action="{{ path('app_training_material_delete', {'id': training_material.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_material.id) }}">
    <button class="btn">Delete</button>
</form>

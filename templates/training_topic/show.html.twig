{% extends 'base.html.twig' %}

{% block title %}TrainingTopic{% endblock %}

{% block body %}
    <h1>TrainingTopic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_topic.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ training_topic.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training_topic.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ training_topic.createdAt ? training_topic.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ training_topic.updatedAt ? training_topic.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_topic_index') }}">back to list</a>

    <a href="{{ path('app_training_topic_edit', {'id': training_topic.id}) }}">edit</a>

    {{ include('training_topic/_delete_form.html.twig') }}
{% endblock %}

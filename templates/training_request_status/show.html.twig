{% extends 'base.html.twig' %}

{% block title %}TrainingRequestStatus{% endblock %}

{% block body %}
    <h1>TrainingRequestStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_request_status.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ training_request_status.status }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training_request_status.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_request_status_index') }}">back to list</a>

    <a href="{{ path('app_training_request_status_edit', {'id': training_request_status.id}) }}">edit</a>

    {{ include('training_request_status/_delete_form.html.twig') }}
{% endblock %}

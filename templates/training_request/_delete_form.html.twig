<form method="post" action="{{ path('app_training_request_delete', {'id': training_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_request.id) }}">
    <button class="btn">Delete</button>
</form>

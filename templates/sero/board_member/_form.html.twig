<form class="form" action="{{ path('board_member_index') }}" method="post"> 

 <div class="form-group row">

	<label class="col-3 col-form-label">With Icon</label>
	<div class="col-3">
		<span class="switch switch-icon">
			<label>
 <input name="status" type="checkbox">
				<span></span>
			</label>
		</span>
	</div>
</div>
<button class="btn btn-light-success btn-sm">{{ button_label|default('Save') }}</button>

 </form>

{# {{form_start(form,  {'attr': {'class': 'form'}}) }}
  <div class="form-group row">

	<label class="col-3 col-form-label">With Icon</label>
	<div class="col-3">
		<span class="switch switch-icon">
			<label>
 				{{form_widget(form.status,  {'attr': {'class': 'switch switch-icon'}}) }}

				<span></span>
			</label>
		</span>
	</div>
</div>

<div class="form-group row">
	<div class="col-9">
	
    {{form_widget(form.user)}}
    </div>
	<label class="col-3 col-form-label">Role</label>
	<div class="col-3">

		{{form_widget(form.role)}}


	</div>
</div>
<button class="btn btn-light-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
